#include <stdio.h>
#include <string.h>
/**
 * main - display the string.0
 * Description: Prints the string to the console.
 * Return: the function value returns 0
 */
int main(void)
{
	char str[50];

	strcpy(str, "\"Programming is like building a multilingual puzzle");
	puts(str);
	return (0);
}
